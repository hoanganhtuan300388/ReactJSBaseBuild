import { connect } from "react-redux";

import component from "../../components/homes/Home"

const mapStateToProps = (state) => {
    return {
    };
};

const mapDispatchToProps = {
};

export default connect(mapStateToProps, mapDispatchToProps)(component);